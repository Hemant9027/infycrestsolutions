export const subscribers = { collection: "subscribers" } as const;
export const projectRequests = { collection: "project_requests" } as const;
export const newCustomers = { collection: "new_customers" } as const;
